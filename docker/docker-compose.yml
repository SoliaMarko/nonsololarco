services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: arco
      POSTGRES_PASSWORD: arco
      POSTGRES_DB: arco_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
