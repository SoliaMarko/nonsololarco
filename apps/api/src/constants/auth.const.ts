/**
 * Temporary auth stub — seed.ts creates a user named "Solomiia".
 * This ID must match the seeded user's `id` in the database.
 *
 * TODO: Replace with JWT auth guard that extracts userId from token.
 * After that, delete this file entirely.
 */
export const MOCK_CURRENT_USER_ID = 'seed-this-after-first-migrate';
