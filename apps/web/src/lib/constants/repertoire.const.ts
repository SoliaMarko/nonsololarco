/** Pseudo-band id for the "All Repertoires" tab — every track where I'm lead member */
export const ALL_BANDS_ID = '';

/** Pseudo-band id for the "Solo" tab — my tracks that have no band */
export const SOLO_BAND_ID = 'solo';
